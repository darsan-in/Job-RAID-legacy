stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cd public
    - cp -r ../* .  # Copy all files to the public directory
  artifacts:
    paths:
      - public  # GitLab Pages serves files from the 'public' directory
  only:
    - main  # Only deploy from the main branch (adjust if needed)
